version: '3.7'

services:
  greengrass:
    init: true
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aws-iot-greengrass
    image: x86_64/aws-iot-greengrass:2.0.0
    ports:
      - "8883:8883"
    entrypoint: "/greengrass-entrypoint.sh"
